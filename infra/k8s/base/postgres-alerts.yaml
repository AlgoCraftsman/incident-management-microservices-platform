apiVersion: v1
kind: Service
metadata:
  name: postgres-alerts
  labels:
    app.kubernetes.io/name: postgres-alerts
    app.kubernetes.io/part-of: incident-platform
spec:
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  selector:
    app.kubernetes.io/name: postgres-alerts
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-alerts
  labels:
    app.kubernetes.io/name: postgres-alerts
    app.kubernetes.io/part-of: incident-platform
spec:
  serviceName: postgres-alerts
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres-alerts
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres-alerts
        app.kubernetes.io/part-of: incident-platform
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          imagePullPolicy: IfNotPresent
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          envFrom:
            - secretRef:
                name: alerts-db-secrets
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "alerts", "-d", "alerts"]
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "alerts", "-d", "alerts"]
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      securityContext:
        seccompProfile:
          type: RuntimeDefault
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
