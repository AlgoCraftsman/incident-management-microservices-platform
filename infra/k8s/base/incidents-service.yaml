apiVersion: v1
kind: Service
metadata:
  name: incidents-service
  labels:
    app.kubernetes.io/name: incidents-service
    app.kubernetes.io/part-of: incident-platform
spec:
  ports:
    - name: http
      port: 8001
      targetPort: http
  selector:
    app.kubernetes.io/name: incidents-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: incidents-service
  labels:
    app.kubernetes.io/name: incidents-service
    app.kubernetes.io/part-of: incident-platform
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: incidents-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: incidents-service
        app.kubernetes.io/part-of: incident-platform
    spec:
      automountServiceAccountToken: false
      containers:
        - name: incidents-service
          image: ghcr.io/algocraftsman/incident-management-microservices-platform/incidents-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8001
          env:
            - name: SERVICE_NAME
              value: incidents-service
            - name: REDIS_URL
              value: redis://redis:6379/0
            - name: EVENT_STREAM_NAME
              value: incident-platform-events
          envFrom:
            - secretRef:
                name: platform-shared-secrets
            - secretRef:
                name: incidents-db-secrets
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: incidents-service
  labels:
    app.kubernetes.io/name: incidents-service
    app.kubernetes.io/part-of: incident-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: incidents-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
