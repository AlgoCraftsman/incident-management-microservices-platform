apiVersion: v1
kind: Service
metadata:
  name: status-page-service
  labels:
    app.kubernetes.io/name: status-page-service
    app.kubernetes.io/part-of: incident-platform
spec:
  ports:
    - name: http
      port: 8004
      targetPort: http
  selector:
    app.kubernetes.io/name: status-page-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: status-page-service
  labels:
    app.kubernetes.io/name: status-page-service
    app.kubernetes.io/part-of: incident-platform
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: status-page-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: status-page-service
        app.kubernetes.io/part-of: incident-platform
    spec:
      containers:
        - name: status-page-service
          image: ghcr.io/algocraftsman/incident-management-microservices-platform/status-page-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8004
          env:
            - name: SERVICE_NAME
              value: status-page-service
            - name: REDIS_URL
              value: redis://redis:6379/0
            - name: EVENT_STREAM_NAME
              value: incident-platform-events
            - name: EVENT_CONSUMER_GROUP
              value: status-page-service
            - name: EVENT_CONSUMER_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          envFrom:
            - secretRef:
                name: platform-shared-secrets
            - secretRef:
                name: status-page-db-secrets
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: status-page-service
  labels:
    app.kubernetes.io/name: status-page-service
    app.kubernetes.io/part-of: incident-platform
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: status-page-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
